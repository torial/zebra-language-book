def main()
    var done: Atomic(bool) = Atomic(bool)(false)

    sys.go(lambda
        # ... do work ...
        sys.sleep(100)
        done.store(true)
    )

    while not done.load()
        sys.sleep(10)

    print("background work finished")