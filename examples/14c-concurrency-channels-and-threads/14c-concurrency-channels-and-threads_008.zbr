def main()
    var done: Atomic(bool) = Atomic(bool)(false)

    sys.go(lambda
        do_work()
        done.store(true)
    )

    while not done.load()
        sys.sleep(10)

    print("worker finished")