def test_concat()
    assert "foo" + "bar" == "foobar"

def test_length()
    assert "hello".len == 5
    assert "".len == 0

def test_contains()
    assert "hello world".contains("world")
    assert not ("hello world".contains("zebra"))