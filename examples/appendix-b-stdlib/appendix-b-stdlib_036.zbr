# Catch expression (inline fallback)
var value = divide(10, 0) catch 0

# Catch with binding
var result = divide(10, 0) catch |e| -1

# Try/catch block
try
    var v = divide(10, 0)
    print(v)
catch |err|
    print("Error: ${err}")